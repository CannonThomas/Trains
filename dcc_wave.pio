.program dcc_wave

.wrap_target
    pull block
    set pins, 0b01      ; GPIO23 HIGH, GPIO24 LOW
    mov x, osr
loop_a:
    jmp x-- loop_a

    pull block
    set pins, 0b10      ; GPIO23 LOW, GPIO24 HIGH
    mov x, osr
loop_b:
    jmp x-- loop_b
.wrap