.program dcc_wave

.wrap_target
    pull block
    set pins, 0b01      ; GPIO23 HIGH, GPIO24 LOW
    mov x, osr
high_loop:
    jmp x-- high_loop

    pull block
    set pins, 0b10      ; GPIO23 LOW, GPIO24 HIGH
    mov x, osr
low_loop:
    jmp x-- low_loop
.wrap