.program dcc_wave

.wrap_target
    pull block
    set pins, 0b10   ; GPIO23=0, GPIO24=1
    mov x, osr
hold_a:
    jmp x-- hold_a

    pull block
    set pins, 0b01   ; GPIO23=1, GPIO24=0
    mov x, osr
hold_b:
    jmp x-- hold_b
.wrap